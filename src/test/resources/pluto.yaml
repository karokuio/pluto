events:
  broker:
    port: 5672
    username: guest
    password: guest
    host: rabbit
  retry:
    interval: 5
    max_attempts: 20
    max_duration: 300

  publish:
    exchange: karoku
  consume:
    queue: templates

docker:
  uri: http://127.0.0.1:4243
